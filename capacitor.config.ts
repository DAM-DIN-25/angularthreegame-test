import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.iescamp.din25.mycapacitorapp',
  appName: 'ThreeGame',
  webDir: 'dist/ThreeGame/browser',
};

export default config;
